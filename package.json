{
  "name": "e2e-ui-tests",
  "version": "1.0.0",
  "main": "index.js",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "report": "npx playwright show-report",
    "report:clear": "rm -rf allure-results && rm -rf playwright-report && rm -rf test-results",
    "test:staging": "NODE_ENV=staging yarn playwright test --project=chromium"
  },
  "dependencies": {
    "@playwright/test": "1.44.1",
    "@types/dotenv-flow": "3.3.0",
    "@types/node": "20.12.12",
    "dotenv-flow": "3.3.0",
    "typescript": "5.3.2"
  }
}
